{
  "name": "@taskflow/shared-types",
  "version": "0.0.1",
  "description": "Shared TypeScript interfaces between frontend and backend",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
