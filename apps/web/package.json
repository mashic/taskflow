{
  "name": "@taskflow/web",
  "version": "1.0.0",
  "description": "TaskFlow Angular 21 web application",
  "private": true,
  "scripts": {
    "ng": "ng",
    "dev": "ng serve",
    "start": "ng serve",
    "build": "ng build",
    "build:prod": "ng build --configuration production",
    "test": "ng test",
    "test:ci": "ng test --watch=false --browsers=ChromeHeadless",
    "lint": "ng lint",
    "lint:fix": "ng lint --fix",
    "e2e": "playwright test"
  },
  "dependencies": {
    "@taskflow/shared-types": "workspace:*"
  },
  "devDependencies": {}
}
