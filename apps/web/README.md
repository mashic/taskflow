# apps/web

Angular 21 web application for TaskFlow.

## Setup

This app will be created with Angular CLI:

```bash
cd apps
ng new web --routing --style=scss --skip-git
```

Then configure for Angular 21 standalone components.
