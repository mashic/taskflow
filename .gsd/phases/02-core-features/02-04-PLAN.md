---
phase: 02-core-features
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/lists/lists.module.ts
  - apps/api/src/lists/lists.controller.ts
  - apps/api/src/lists/lists.service.ts
  - apps/api/src/lists/lists.repository.ts
  - apps/api/src/lists/dto/create-list.dto.ts
  - apps/api/src/lists/dto/update-list.dto.ts
  - apps/api/src/lists/dto/reorder-list.dto.ts
  - apps/api/src/tasks/tasks.module.ts
  - apps/api/src/tasks/tasks.controller.ts
  - apps/api/src/tasks/tasks.service.ts
  - apps/api/src/tasks/tasks.repository.ts
  - apps/api/src/tasks/dto/create-task.dto.ts
  - apps/api/src/tasks/dto/update-task.dto.ts
  - apps/api/src/tasks/dto/move-task.dto.ts
  - apps/api/src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "Authenticated user can create lists in their boards"
    - "Authenticated user can create tasks in lists"
    - "Lists maintain position ordering"
    - "Tasks maintain position ordering within lists"
    - "Moving tasks updates only affected positions (fractional)"
  artifacts:
    - path: "apps/api/src/lists/lists.repository.ts"
      provides: "List data access"
      exports: ["ListsRepository"]
    - path: "apps/api/src/tasks/tasks.repository.ts"
      provides: "Task data access"
      exports: ["TasksRepository"]
    - path: "apps/api/src/lists/lists.controller.ts"
      provides: "List REST endpoints"
      exports: ["ListsController"]
    - path: "apps/api/src/tasks/tasks.controller.ts"
      provides: "Task REST endpoints"
      exports: ["TasksController"]
  key_links:
    - from: "lists.controller.ts"
      to: "boards.service.ts"
      via: "board ownership check"
    - from: "tasks.controller.ts"
      to: "lists.repository.ts"
      via: "list existence check"
---

<objective>
Implement backend List and Task CRUD with position-based ordering using fractional indices.

Purpose: API endpoints for managing lists and tasks within boards
Output: ListsModule and TasksModule with position ordering
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/STATE.md
@.gsd/phases/02-core-features/02-RESEARCH.md
@.gsd/phases/02-core-features/02-01-SUMMARY.md

# Follow board patterns
@apps/api/src/boards/boards.repository.ts
@apps/api/src/boards/boards.controller.ts
@packages/shared-types/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>02-04-T1: Add List and Task models to Prisma schema</name>
  <files>apps/api/prisma/schema.prisma</files>
  <action>
    Add List model:
    - id: String @id @default(cuid())
    - title: String
    - boardId: String (relation to Board)
    - position: Float (fractional index for ordering)
    - createdAt, updatedAt
    - tasks: Task[] relation
    
    Add Task model:
    - id: String @id @default(cuid())
    - title: String
    - description: String?
    - listId: String (relation to List)
    - boardId: String (denormalized for efficient queries)
    - position: Float (fractional index)
    - assigneeId: String? (relation to User)
    - dueDate: DateTime?
    - priority: String? (low, medium, high, urgent)
    - createdAt, updatedAt
    
    Add relations to Board model:
    - lists: List[]
    - tasks: Task[]
    
    Run `pnpm --filter api prisma generate` and `pnpm --filter api prisma db push`
  </action>
  <verify>
    Prisma generates List and Task types
    DB schema includes new tables
  </verify>
  <done>List and Task models exist with Float position field</done>
</task>

<task type="auto">
  <name>02-04-T2: Create ListsModule with position ordering</name>
  <files>
    apps/api/src/lists/lists.module.ts
    apps/api/src/lists/lists.repository.ts
    apps/api/src/lists/lists.service.ts
    apps/api/src/lists/lists.controller.ts
    apps/api/src/lists/dto/create-list.dto.ts
    apps/api/src/lists/dto/update-list.dto.ts
    apps/api/src/lists/dto/reorder-list.dto.ts
  </files>
  <action>
    1. Create DTOs:
       - CreateListDto: title, boardId
       - UpdateListDto: title?
       - ReorderListDto: position (new position value)
    
    2. ListsRepository methods:
       - create(data): creates list with position = max(existing) + 1
       - findByBoard(boardId): returns lists ordered by position
       - findById(id): single list
       - update(id, data): updates list
       - reorder(id, newPosition): updates position (fractional)
       - delete(id): removes list
    
    3. ListsService:
       - Verify board ownership before operations
       - calculatePosition(boardId, targetIndex): returns fractional position
         - If inserting between positions 2.0 and 3.0 → return 2.5
         - If inserting at end → return max + 1
    
    4. ListsController:
       - @Post('boards/:boardId/lists'): create list in board
       - @Get('boards/:boardId/lists'): get lists for board
       - @Patch('lists/:id'): update list
       - @Patch('lists/:id/reorder'): reorder list
       - @Delete('lists/:id'): delete list
    
    5. Create ListsModule, import in AppModule
  </action>
  <verify>
    `pnpm --filter api build` compiles
    List endpoints accessible
  </verify>
  <done>ListsModule with position-based ordering exists</done>
</task>

<task type="auto">
  <name>02-04-T3: Create TasksModule with move support</name>
  <files>
    apps/api/src/tasks/tasks.module.ts
    apps/api/src/tasks/tasks.repository.ts
    apps/api/src/tasks/tasks.service.ts
    apps/api/src/tasks/tasks.controller.ts
    apps/api/src/tasks/dto/create-task.dto.ts
    apps/api/src/tasks/dto/update-task.dto.ts
    apps/api/src/tasks/dto/move-task.dto.ts
  </files>
  <action>
    1. Create DTOs:
       - CreateTaskDto: title, description?, listId, boardId, priority?, dueDate?
       - UpdateTaskDto: title?, description?, priority?, dueDate?, assigneeId?
       - MoveTaskDto: listId, position (move to list at position)
    
    2. TasksRepository methods:
       - create(data): creates task with position = max in list + 1
       - findByList(listId): tasks ordered by position
       - findByBoard(boardId): all tasks in board with list info
       - findById(id): single task
       - update(id, data): updates task fields
       - move(id, listId, position): moves task to list at position
       - delete(id): removes task
    
    3. TasksService:
       - Verify board ownership via list → board chain
       - moveTask(id, dto): handles same-list reorder or cross-list move
       - Use fractional positions to avoid cascade updates
    
    4. TasksController:
       - @Post('lists/:listId/tasks'): create task in list
       - @Get('boards/:boardId/tasks'): all tasks in board
       - @Get('tasks/:id'): single task
       - @Patch('tasks/:id'): update task
       - @Patch('tasks/:id/move'): move task
       - @Delete('tasks/:id'): delete task
    
    5. Create TasksModule, import in AppModule
  </action>
  <verify>
    `pnpm --filter api build` compiles
    Task move endpoint calculates fractional position
  </verify>
  <done>TasksModule with move and position ordering exists</done>
</task>

</tasks>

<verification>
```bash
pnpm --filter api start:dev

# Create a list
curl -X POST http://localhost:3000/boards/$BOARD_ID/lists \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"title": "To Do"}' -H "Content-Type: application/json"

# Create a task
curl -X POST http://localhost:3000/lists/$LIST_ID/tasks \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"title": "First Task", "boardId": "'$BOARD_ID'"}' -H "Content-Type: application/json"

# Move task
curl -X PATCH http://localhost:3000/tasks/$TASK_ID/move \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"listId": "'$LIST_ID'", "position": 1.5}' -H "Content-Type: application/json"
```
</verification>

<success_criteria>
1. List model has Float position field
2. Task model has Float position and listId/boardId
3. Creating lists auto-assigns incrementing positions
4. Moving tasks only updates the moved task's position (not cascade)
5. /boards/:id/tasks returns all tasks grouped by list
6. Board ownership checked for all list/task operations
</success_criteria>

<output>
After completion, create `.gsd/phases/02-core-features/02-04-SUMMARY.md`
</output>
